# ASAS Session State

**Repository:** `asas-erp-saas-1/As`  
**Canonical integration branch:** `main`  
**Last updated:** 2026-09-16

## Founder mission

Prepare and harden the ASAS repository so Claude Code can autonomously develop the ASAS application safely and professionally once each relevant gate is closed. Foundation work exists to remove ambiguity and establish executable contracts, evidence, safety gates, task packets and autonomy rules; it is not documentation for its own sake.

## Canonical project identity

- Canonical repository: `asas-erp-saas-1/As`.
- `asas-erp-saas-1/Asas-website` is a separate/unrelated project and must never be used as evidence, codebase, deployment, database, migration history or product state for ASAS.
- Supabase project `asas-web-site` is a separate/unrelated project and is not ASAS database reality.
- Actual Supabase project identity for `As`: NOT VERIFIED.
- Actual Vercel project identity for `As`: NOT VERIFIED.

## Repository state

- Foundation governance and Claude handoff controls are now merged into `main` in squash commit `c5849e0f212a1630e121facd55ba866dfaf3953a`.
- Repository remains intentionally pre-implementation: no application code, database schema, migrations or production mutations have been authorized by this checkpoint.
- Canonical handoff includes repository identity, Claude context index, implementation boundary, Master Execution Path, task graph model, reconciliation protocol, repository map, branch/PR policy and foundation verification workflow.
- `foundation/repository-governance` remains a historical foundation branch after merge and is not the integration truth.

## Foundation controls completed

- Canonical repository identity and absolute project boundary.
- Claude startup/context loading and autonomy model.
- Task packet and dependency-aware execution model.
- Contract authority and contradiction handling.
- Core lifecycle and capability coverage controls.
- Branch/PR governance and branch hygiene register.
- Foundation CI verification contract and workflow.
- Repository handoff map and placement rules.
- Claude-ready repository closure standard.

## Current gate state

- G0 Repository identity: GREEN / VERIFIED.
- G1 Platform identity: BLOCKED / external runtime evidence required.
- G2 Contract authority/reconciliation: GREEN for repository-level policy; domain slices remain task-scoped.
- G3 Task graph/task packets: AMBER; canonical machine-readable registers still require controlled promotion/reconciliation.
- G4 CI: PARTIAL; foundation verification exists, application-stack checks await the actual implementation stack.
- G5 Security/tenancy: AMBER; policy is established, implementation evidence does not exist yet.
- G6 Database/migration safety: BLOCKED until runtime/database reality is verified.
- G7 Claude autonomy for application/runtime work: NOT AUTHORIZED globally; task-scoped authorization follows closed gates.

## Current highest-priority work

1. Reconcile and promote validated machine-readable contracts from the supplied source package into `As` without altering semantics.
2. Close remaining domain contract gaps (including B.4 and open policy predicates) before the affected implementation slice.
3. Complete command ↔ permission ↔ ABAC ↔ state-edge ↔ event traceability.
4. Establish tenant-negative, invariant, concurrency and security verification contracts.
5. When runtime evidence is connected, perform GATE-00 read-only platform/database reconciliation.
6. Convert the dependency graph into implementation-ready task packets.
7. Open the first executable vertical slice only when its applicable gates are GREEN.

## Explicit boundary

Repository workspace readiness is distinct from runtime/database/production readiness. Claude may use the repository as its canonical engineering workspace, but must not infer blanket authorization for schema, migration, deployment or production operations.

## Resume rule

When the founder says `أكمل عملك على المسار`, reload this Session State, the Master Execution Path, the canonical handoff/governance files, and the current task graph from the repository. Inspect the current `main` state before acting; continue from the first unresolved dependency; do not restart completed work from conversational memory.
